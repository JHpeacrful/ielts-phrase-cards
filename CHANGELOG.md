# 更新说明

## 2026-09-09 · JSON 导入修复

- JSON 导入兼容顶层数组、`cards`、`data`、`items`、`phrases`、`records` 和 `entries` 数据结构，也支持直接导入单张卡片。
- 支持常见中文字段名（短语、提示、释义、固定搭配、语法框架、例句），导入时自动补齐缺失的提示、标签和状态。
- 文件读取失败、JSON 格式错误或无有效卡片时显示具体原因，不再表现为无响应。

## 2026-09-09 · 复习隔离、收藏夹与多词库

- 复习卡只在“复习”视图显示；切换到概览或学习时暂停，回到复习页继续当前卡片。
- 复习页支持按词库选择，每个词库独立计算到期和评分；复习正面与背面均可用五角星收藏。
- 复习页新增范围选择，可在当前词库中复习全部卡片或仅复习收藏卡片。
- 历史页增加“全部卡片 / 收藏夹”子标签，收藏夹汇总所有词库并保留搜索、筛选、编辑、删除。
- 卡片支持加入多个词库；设置页新增词库创建、重命名和删除管理。
- 导入扩展为 JSON、CSV、TXT；普通导入前选择已有词库或新建词库。
- macOS App 增加粘贴网页文字的 AI 整理导入：先生成可编辑预览，待确认行可手动补全，确认后新建独立词库。
- 设置页新增每日文案风格：冷静克制、温柔鼓励、考试冲刺、简短有力；AI 失败时使用内置文案兜底，并在设置页提示。

## 2026-09-09 · AI 生成确认与字段约束

- 点击字段旁的 AI 按钮时只打开工作台，不再自动发起生成请求；必须点击“生成内容”后才会调用 API。
- 固定搭配/语法框架字段新增严格格式约束：只返回结构模板，禁止完整例句、中文解释和 Markdown。
- 原创句子字段要求一次返回一行口语句和一行书面句，均正确使用目标短语，按 IELTS 7-8 分段控制自然度、准确度和正式程度。
- 增加生成结果校验：固定搭配疑似返回例句时拒绝应用；原创句子缺少两行标签时要求重新生成。

## 2026-09-08 · GitHub Pages 网页版同步

- 将最新的五视图界面同步到仓库根目录 `index.html`，GitHub Pages 现在会直接加载概览、学习、复习、历史、设置导航。
- 网页版继续保留本地卡片保存、复习算法、正反面切换、左右导航、深色模式和 JSON 导入导出。
- macOS App 的 AI 配置与字段工作台仍保留在 `macos-app/`；纯网页环境会安全地禁用需要 Electron 桥接的 AI 操作。

## 2026-09-08 · macOS App 分区导航与仓库同步

- 新增 macOS App 源码目录 `macos-app/`，与网页版并行保留。
- App 新增概览、学习、复习、历史、设置五个固定顶部标签；窄窗口自动收缩为当前页面下拉菜单。
- 概览增加今日到期、今日完成进度和连续学习天数；学习草稿在切换标签时保留。
- 复习页只显示今日到期卡，保留正反面、左右切换和 R/Y/G 评分算法。
- 设置页拆分为外观、AI 配置、数据备份三个区块；AI 结果继续采用“先预览、后应用”。
- README 重写为项目首页说明，补充 App 运行、打包、AI 配置、数据隐私和迁移说明。
- 未提交约 257 MB 的 Electron `.app` 二进制，避免超过 GitHub 单文件大小限制；可通过 `npm run dist` 本地构建。

## 2026-08-25 · 暖编辑部 UI 新版

- 重做页面视觉系统：暖纸白、近黑墨色和朱砂橙强调色，减少通用后台感。
- 调整深色模式为中性石墨黑配色，降低青灰感并提升暖色强调的辨识度。
- 优化标题、统计区、表单、复习面板、卡片列表和备份区的层级与间距。
- 增加更清晰的悬停、焦点、禁用和深色主题状态，并支持减弱动画偏好。
- 保留原有卡片数据结构、复习算法、正反面切换、左右导航与导入导出功能。

## 2026-08-25

- 新增深色模式切换按钮，支持在深色模式和浅色模式之间切换。
- 主题选择保存在当前浏览器，刷新页面后仍会保持。
- 深色模式覆盖表单、复习面板、卡片和 G/Y/R 状态标签配色。
- 修复深色模式下输入占位文字对比度不足的问题。

## 2026-08-19 · GitHub Pages

- 修复 GitHub Pages 发布源被设置为 `None` 导致网站返回 404 的问题。
- 恢复从 `main` 分支根目录发布，线上地址重新可访问。

## 2026-08-19

- 复习模式背面新增“返回正面”按钮。
- 正面和背面都新增“← 上一条”和“下一条 →”按钮，可在本轮复习卡片之间切换。
- 切换卡片时默认显示目标卡片正面，避免直接泄露答案。
- 修复评分后“上一条”始终禁用的问题：本轮卡片不再因评分被移除，已评分卡片可以返回查看。
- 已评分卡片不会重复累计复习次数或重复修改到期日期。
- 复习进度显示当前卡片位置和剩余未评分数量。
